#!/bin/sh

if ! tmux ls &> /dev/null; then
  tmux new-session -d 'su - hass'

  # We don't want hass running as the top-command in tmux, since the pane will
  # die if it exits.  
  tmux send-keys 'hass --config /config' Enter
fi

tmux attach
